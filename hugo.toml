baseURL = 'https://akemarropa.github.io/blog/'
languageCode = 'en'
title = 'ake blog'
theme = 'blowfish'

# Configuración de URLs y SEO
relativeURLs = true
canonifyURLs = true
buildFuture = true

# ELIMINAR ETIQUETAS Y CATEGORÍAS (Minimalismo)
[taxonomies]
  tag = ""
  category = ""

# URLs LIMPIAS (Sin fechas)
[permalinks]
  posts = "/posts/:slug/"