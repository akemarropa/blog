baseURL = 'https://akemarropa.github.io/blog/'
theme = 'blowfish'
title = 'ake blog'
languageCode = 'es'

# Esto es lo que le falta a Blowfish para pintar el contenido
[outputs]
  home = ["HTML", "RSS", "JSON"]
  page = ["HTML"]
  section = ["HTML", "RSS"]

[params]
  defaultAppearance = "dark"