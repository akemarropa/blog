baseURL = 'https://akemarropa.github.io/blog/'
languageCode = 'es'
title = 'ake blog'
theme = 'blowfish'

[params]
  defaultAppearance = "dark"

# ELIMINAMOS la sección [permalinks] que pusimos antes 
# porque es lo que está haciendo que el post salga vacío.