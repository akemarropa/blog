baseURL = 'https://akemarropa.github.io/blog/'
theme = 'blowfish'
title = 'ake blog'
languageCode = 'es'
defaultContentLanguage = 'es'

[params]
  defaultAppearance = "dark"
  # Esto fuerza a que el contenido se vea aunque haya líos de idioma
  mainSections = ["posts"]

[outputs]
  home = ["HTML", "RSS", "JSON"]
  page = ["HTML"]
  section = ["HTML", "RSS"]